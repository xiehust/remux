package dev.remux.core

/**
 * Top-level constants for the Remux core module (pure Kotlin/JVM, no Android
 * dependencies). The terminal emulator, ssh_config/tmux parsers, tunnel
 * protocol, and AI-command catalog are added in later phases.
 */
object Remux {
    const val NAME: String = "Remux"
    const val TAGLINE: String = "Tap into your AI agents, anywhere."
}
