// Package e2e holds the end-to-end reverse-tunnel integration test that drives
// real SSH bytes app -> relay -> agent -> sshd with tmux. The test itself is
// added in phase 4 (build-tagged) alongside scripts/e2e.sh.
package e2e
